apiversion: v1
kind: Service
metadata:
  name: backend-service
  namespace: emp

spec:
  Ports:
    - name: backend
      port: 8080
      targetPort: 8080
  
  selector:
    app: backend
  type: ClusrteIP
